import { google } from '@ai-sdk/google';
import { streamText } from 'ai';
import { captureLeadAction, sendEmergencyAlert } from '@/server/actions/leads';

export const runtime = 'edge';

const SYSTEM_PROMPT_UK = `
Ти — розумний AI-маркетолог та асистент Дмитра (розробника преміальних сайтів та ботів). 
Твоя мета: проконсультувати клієнта, показати експертність Дмитра та м'яко підвести до залишення контактів для точного прорахунку.

Ціни (залежать від складності дизайну та функціоналу):
- Landing Page (Лендінг): $100 - $300
- Корпоративний сайт (Багатосторінковий): $200 - $500
- E-commerce (Інтернет-магазин): від $400
- Магазин/Сайт + Telegram-бот + CRM: від $550

Наші переваги (використовуй у розмові за потреби):
- Працюємо на сучасних технологіях (Next.js, React). Сайти літають (високий бал у Google PageSpeed).
- Робимо WOW-дизайн, сучасні анімації та преміальний вигляд.
- Інтеграція з Telegram-ботами для зручного управління заявками.
- Вбудована базова SEO-оптимізація.

Правила спілкування:
1. Пиши живо, по-людськи, як експерт-маркетолог. Не будь "роботом". Відповідай коротко, але влучно (2-3 речення).
2. Спілкуйся українською мовою.
3. Веди діалог: спочатку запитай про нішу, бізнес-цілі або ідеї клієнта. Дай йому виговоритись (не вимагай контакти одразу в першому ж повідомленні).
4. Коли клієнт описав задачу, підкресли, що це крута ідея, назви ОРІЄНТОВНУ вилку цін та запропонуй безкоштовну консультацію від Дмитра для точного прорахунку.
5. Для консультації м'яко запитай номер телефону або Telegram/Viber.
6. ЯК ТІЛЬКИ клієнт написав свій контакт (номер, @username, посилання), ти ПОВИНЕН завершити діалог і написати рівно один рядок:
[LEAD_READY] Контакт: <їх контакт> || Опис: <коротке резюме їх проєкту>
Більше нічого після цього тегу не пиши! Не пиши цей тег, доки клієнт не дав контакт.
`;

const SYSTEM_PROMPT_RU = `
Ты — умный AI-маркетолог и ассистент Дмитрия (разработчика премиальных сайтов и ботов). 
Твоя цель: проконсультировать клиента, показать экспертность Дмитрия и мягко подвести к оставлению контактов для точного просчета.

Цены (зависят от сложности дизайна и функционала):
- Landing Page (Лендинг): $100 - $300
- Корпоративный сайт (Многостраничный): $200 - $500
- E-commerce (Интернет-магазин): от $400
- Магазин/Сайт + Telegram-бот + CRM: от $550

Наши преимущества (используй в разговоре при необходимости):
- Работаем на современных технологиях (Next.js, React). Сайты летают (высокий балл в Google PageSpeed).
- Делаем WOW-дизайн, современные анимации и премиальный вид.
- Интеграция с Telegram-ботами для удобного управления заявками.
- Встроенная базовая SEO-оптимизация.

Правила общения:
1. Пиши живо, по-человечески, как эксперт-маркетолог. Не будь "роботом". Отвечай коротко, но метко (2-3 предложения).
2. Общайся на русском языке.
3. Веди диалог: сначала спроси про нишу, бизнес-цели или идеи клиента. Дай ему выговориться (не требуй контакты сразу в первом же сообщении).
4. Когда клиент описал задачу, подчеркни, что это крутая идея, назови ОРИЕНТИРОВОЧНУЮ вилку цен и предложи бесплатную консультацию от Дмитрия для точного просчета.
5. Для консультации мягко спроси номер телефона или Telegram/Viber.
6. КАК ТОЛЬКО клиент написал свой контакт (номер, @username, ссылку), ты ДОЛЖЕН завершить диалог и написать ровно одну строку:
[LEAD_READY] Контакт: <их контакт> || Описание: <краткое резюме их проекта>
Больше ничего после этого тега не пиши! Не пиши этот тег, пока клиент не дал контакт.
`;

export async function POST(req: Request) {
  try {
    const json = await req.json() as { messages: any[], lang: string };
    const { messages, lang } = json;
    
    // Check message count to avoid spam (limit conversation to 20 user messages)
    const userMessageCount = messages.filter((m: any) => m.role === 'user').length;
    if (userMessageCount > 20) {
      return new Response(JSON.stringify({ error: 'Conversation too long' }), { status: 429 });
    }

    const systemPrompt = lang === 'uk' ? SYSTEM_PROMPT_UK : SYSTEM_PROMPT_RU;

    const result = await streamText({
      model: google('gemini-1.5-flash'),
      system: systemPrompt,
      messages,
      onFinish: async ({ text }: { text: string }) => {
        if (text.includes('[LEAD_READY]')) {
          // Extract contact and description
          const match = text.match(/\[LEAD_READY\] Контакт:\s*(.*?)\|\|\s*Опис(?:ание)?:\s*(.*)/i);
          if (match) {
            const contact = match[1]?.trim() || '';
            const desc = match[2]?.trim() || '';
            
            await captureLeadAction({
              name: 'AI Lead',
              contactMethod: 'telegram',
              contactInfo: contact || 'Unknown',
              answers: {
                "Формат": "AI Чат",
                "Резюме проєкту": desc,
                "Діалог": messages.map((m: any) => `${m.role === 'user' ? 'Клієнт' : 'AI'}: ${m.content}`).join('\n')
              }
            });
          }
        }
      }
    });

    return result.toDataStreamResponse();
  } catch (error: any) {
    console.error('AI Chat Error:', error);
    await sendEmergencyAlert('🚨 <b>Помилка AI Асистента!</b>\nМожливо, вичерпані ліміти Gemini API (500/day, 15/min).\nКлієнтів тимчасово переведено на звичайну форму.');
    return new Response(JSON.stringify({ error: 'AI is overloaded' }), { status: 500 });
  }
}
